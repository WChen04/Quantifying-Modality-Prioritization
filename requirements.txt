# Core dependencies
pillow>=10.0.0
gTTS>=2.4.0
pandas>=2.0.0
pyyaml>=6.0
tqdm>=4.66.0

# Hugging Face dependencies
transformers>=4.40.0
accelerate>=0.20.0
sentencepiece>=0.1.99
protobuf>=4.25.0

# PyTorch (will be installed separately based on platform)
# For CUDA: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# For CPU/Mac: pip install torch torchvision torchaudio

# Optional: 4-bit quantization (NVIDIA GPU only)
# bitsandbytes>=0.41.0